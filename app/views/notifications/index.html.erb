<h3>通知</h3>
  <%= link_to "全削除", user_destroy_all_path, method: :delete %>

  <% if @notifications.exists? %>
    <div>
      <%= render @notifications %>
    </div>
  <% else %>
      <p>通知はありません</p>
  <% end %>
