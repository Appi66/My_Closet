<h1>OOTD投稿フォーム</h1>
  <%= form_with model: @post_image do |f| %>
    <h4>Image</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>Caption</h4>
      <%= f.text_area :caption %>
    <%= f.submit 'Post' %>
  <% end %>

